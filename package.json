{
  "name": "agenturn",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace=packages/dashboard",
    "dev:bot": "npm run dev --workspace=packages/bot"
  }
}
